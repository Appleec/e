import { describe, it, test } from 'vitest';

import base from '../src/configs/node';

describe('e-eslint-config', () => {
  it('should nothing', () => {
    console.log('=>', base);
  });
  // test.todo('write some test for this module');
});
